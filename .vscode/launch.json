{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Open Habit Tracker in Browser",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8000",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Open Habit Tracker"
        }
    ]
}
